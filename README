# Lleuad

A statically typed scripting language.

```lleuad
use library(std).List;

function[T] sum(xs: List[T]): T begin
    local accum = 0;
    for x in xs begin
        accum = accum + x;
    end;
end;
```
